extends layout

block content
    h1.username(type=hidden) #{username}
    h1.roomname(type=hidden) #{room}
    ul.pages
        li.chat.page
            div.chatArea
                ul.messages
                input.inputMessage(placeholder="Type here...")
